// firmware/src/secrets.h.example
// Copy this file to secrets.h (same directory) and fill in your values.
// secrets.h is gitignored — never commit real credentials.

#pragma once

#define WIFI_SSID  "your_network_name"
#define WIFI_PASS  "your_network_password"
#define HOST_IP    "192.168.x.x"   // Raspberry Pi LAN IP
